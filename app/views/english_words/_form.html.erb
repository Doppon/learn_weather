<%= form_with(model: english_word, local: true) do |form| %>
  <% if english_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(english_word.errors.count, "error") %> prohibited this english_word from being saved:</h2>

      <ul>
      <% english_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">
    <div class="form-group">
      <%= form.label :word %>
      <%= form.text_field :word, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :word_meaning %>
      <%= form.text_field :word_meaning, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :sentence %>
      <%= form.text_area :sentence, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :sentence_meaning %>
      <%= form.text_area :sentence_meaning, class: "form-control" %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  </div>
<% end %>
